# Example Environment Variables
# IMPORTANT: These are example values only - REPLACE with your real credentials!
# Copy this file to .env and update with your actual configuration

# Application Configuration
APP_NAME=Event Admin Example
APP_ADMIN_USER=demo_admin
APP_ADMIN_PASS=demo_password_123

# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_NAME=example_events_db
DB_USER=db_user_example
DB_PASSWORD=db_password_example

# SSH Tunnel Configuration (if using remote database)
SSH_HOST=ssh.example.com
SSH_USER=example_user
SSH_PASS=example_ssh_password
